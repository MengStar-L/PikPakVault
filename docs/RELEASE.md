TelDrive 文件移出原同步目录或重命名后保持同一资源身份，不重复上传；PikPak 副本缺失后恢复到资源库中的最新位置。

- 已登记资源独立于原目录检查，原空目录被删除或源文件移出监控目录后，仍按保存的 TelDrive 文件 ID 追踪。
- 缺失恢复跟随监控模式：自动监控自动补回；手动监控点击“立即同步”或在恢复中心确认后补回。分页不完整、状态未知、位置差异和首次上传未完成不会触发重复上传。
- 共用回收站还原和 aria2 重新下载上传流程，只恢复确认缺失的条目。移动、重命名及祖先目录调整不丢失收藏、播放记录或来源信息。
- 下载或上传过程中调整路径时重新读取最新目标；响应丢失、进程重启及切号保留原上传会话。目录创建响应丢失后从原请求位置核对结果，避免重复创建目录。
- 本地主动回收、来源失效和同名内容冲突均保留原有保护及具体原因。已有上传、恢复和路径调整任务不会被新的同步任务抢占。

数据库沿用 schema 2，无需迁移。升级保留账号认证、目录、来源、原任务、专用 aria2 与下载缓存。从 0.3.1 或更早版本升级仍需先执行新版包中的 deploy/prepare-runtime.sh 配置专用运行目录。

验证：Go 全量测试、静态检查、前端构建和 19 项浏览器测试通过。新增回归覆盖移动后去重、最新路径恢复、原目录删除、手动与自动模式、分页中断、传输中移动、响应丢失、重启、切号和本地回收站。
